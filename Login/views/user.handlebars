<style>
    button {
        border-radius: 20px;
        border: 1px solid #ec9c24;
        background-color: #ec9c24;
        color: #FFFFFF;
        font-size: 12px;
        font-weight: bold;
        padding: 0px;
        letter-spacing: 1px;
        text-transform: uppercase;
        transition: transform 80ms ease-in;
    }

    button:active {
        transform: scale(0.95);
    }

    button:focus {
        outline: none;
    }
    .btn1{
        padding: 0px;
    }
</style>

<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="#" class="logo_brand">
            <h2>Redis</h3>
          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="./#">Home</a>
              </li>
              {{!-- <li class="nav-item">
                <a class="nav-link" href="/user/add">Add User</a>
              </li> --}}
          </div>
        </div>
      </nav>
</header>

<body>
    <div class="container" id="container">

        <h1>Xin chào, {{user.id}}</h1>
        <ul class="list-group">
        <li class="list-group-item"><strong>Email: </strong>{{user.email}}</li>
        <li class="list-group-item"><strong>Phone: </strong>{{user.phone}}</li>
        </ul>
        <br>    
        <form method="GET" class="btn btn-primary pull-left" action="/user/update/{{user.id}}?_method=GET">
            <button class="btn btn-primary pull-left btn1"><b> Cập nhật</b></button>
        </form>
        
        <button>
            <a class="btn btn-default pull-left" href="/"><b> Thoát</b></a>
        </button>
        

    </div> 
</body>   